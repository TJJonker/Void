#include "pch.h"
#include "TextureLoader.h"

namespace Void {
    Texture* Void::TextureLoader::LoadTexture(const std::string& filePath)
    {
        //Texture* texture = Texture::Create();
        return nullptr;
    }
}
