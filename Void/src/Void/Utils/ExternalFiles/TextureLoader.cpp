#include "pch.h"
#include "TextureLoader.h"

namespace Void {
    Rendering::Texture* TextureLoader::Load(const std::string& filePath)
    {
        //Texture* texture = Texture::Create();
        return nullptr;
    }
}
